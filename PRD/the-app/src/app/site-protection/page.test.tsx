import SiteProtectionPage from '../page';

describe('SiteProtectionPage', () => {
  it('should be defined', () => {
    expect(SiteProtectionPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;