import SystemOverridePage from '../page';

describe('SystemOverridePage', () => {
  it('should be defined', () => {
    expect(SystemOverridePage).toBeDefined();
  });
});

const testExport = {};
export default testExport;