import EditTownPage from '../page';

describe('EditTownPage', () => {
  it('should be defined', () => {
    expect(EditTownPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;