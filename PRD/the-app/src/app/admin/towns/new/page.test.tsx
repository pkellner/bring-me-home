import NewTownPage from '../page';

describe('NewTownPage', () => {
  it('should be defined', () => {
    expect(NewTownPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;