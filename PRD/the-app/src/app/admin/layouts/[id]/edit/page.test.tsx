import EditLayoutPage from '../page';

describe('EditLayoutPage', () => {
  it('should be defined', () => {
    expect(EditLayoutPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;