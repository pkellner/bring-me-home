import LayoutsPage from '../page';

describe('LayoutsPage', () => {
  it('should be defined', () => {
    expect(LayoutsPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;