import NewLayoutPage from '../page';

describe('NewLayoutPage', () => {
  it('should be defined', () => {
    expect(NewLayoutPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;