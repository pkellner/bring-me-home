import NewUserPage from '../page';

describe('NewUserPage', () => {
  it('should be defined', () => {
    expect(NewUserPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;