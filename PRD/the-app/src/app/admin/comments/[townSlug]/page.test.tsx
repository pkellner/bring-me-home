import TownCommentsPage from '../page';

describe('TownCommentsPage', () => {
  it('should be defined', () => {
    expect(TownCommentsPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;