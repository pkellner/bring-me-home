import NewPersonPage from '../page';

describe('NewPersonPage', () => {
  it('should be defined', () => {
    expect(NewPersonPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;