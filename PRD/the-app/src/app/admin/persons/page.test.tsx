import PersonsPage from '../page';

describe('PersonsPage', () => {
  it('should be defined', () => {
    expect(PersonsPage).toBeDefined();
  });
});

const testExport = {};
export default testExport;